charCodeAt() -> 해당 문자열 번호를 숫자로 변경해줌

DP -> 다이나믹프로그래밍? N번째 요소를 구할때 N-1까지 합곱 등등을 구해서 N번째를 구한다?

해시맵과 셋
해시맵 -> 중복원소 허용안함
해시셋 -> 들어가는 값은 단일값 


1. hash_set 자료구조의 특징
	hash_set은 hash_map과 마찬가지로 자료를 트리에 정렬하여 저장하는 형태가 아니라
	해쉬라는 자료구조를 통해 map, set보다 검색속도를 높인 컨테이너입니다.
	그렇기 때문에 반복자로 저장된 데이터를 순회할 때 넣은 순서대로 순회하지 않고
	정렬된 순서로 순회하게 됩니다..



같은키에 다른값이 들어가면 오버라이드를 함 ~~ 자바도 오버라이드를 함

teacherschool.net/enrollment