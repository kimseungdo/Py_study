fibo memorization ~~ 

깊이와 크기를 구함 -> *=2 깊이 // 124816~~ + 크기
노드가 끝까지 갔는지 비교 || 값이 대칭인지 비교 || 리턴 조건을 노드 위치 참조를 통해 계속 함수 호출

containsKey -> 위치값을 기억한다라? 값을뽑아내면 get이나 containsKey랑 같지않나? 가독성문제?
containsKey
||
맵을 순회하지 않는다

hashmap 클래스 실제 구현할수있는 것
map은 자료형을 구현해논 인터페이스
자료형도 클래스도 해시맵으로 가능한가? 

자바스크립트는 해시맵구조는 없으나 클래스가 해줌